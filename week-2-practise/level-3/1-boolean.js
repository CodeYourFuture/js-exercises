// The code is valid but is misleading and could be improved
// Refactor the code to make it better
// What was wrong with the original code?
// The issue was the double quote.
// What was wrong was the double quote.
// Boolean doesn't take quote.
// Leave comments above your changes to explain.
// We need to print "I am not happy" and it is false so we put false.
// Why i change it because Boolean does not take quote.
// Because it will considere as a string.

var isHappy = false;

if (isHappy) {
  console.log("I am happy");
} else {
  console.log("I am not happy");
}

/* 
  EXPECTED RESULT
  ---------------
  I am not happy
*/
