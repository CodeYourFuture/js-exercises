// Update the variable `sortedLetters` so that it contains the values of `letters` in ascending order
// TIP: use an array method

var letters = ["a", "f", "d", "n", "z", "c"];
var sortedLetters = letters.sort(); // ONLY EDIT THIS LINE

console.log(sortedLetters);

/* 
  EXPECTED RESULT
  ---------------
  [ 'a', 'c', 'e', 'f', 'n', 'z' ]
*/
/*
(function(cur, nex) {
  if (cur.name < nex.name) return -1;
  if (cur.name > nex.name) return 1;
  return 0;
  */
