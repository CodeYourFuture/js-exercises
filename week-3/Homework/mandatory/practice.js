function validate(password){

if (password.match(/[a-z]/) && password.match(/[A-Z]/) && match.includes(/\d+/)
&& (match.length))
  return /(true)/.test(password);
}

  console.log(validate('djI38D55'));