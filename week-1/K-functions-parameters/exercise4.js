// Declare your function first - call it "add"

// Call the function and assign to a variable `sum`
function add(a, b) {
  return a + b;
}
var sum = add(13, 124);
console.log(sum);

// ignore below

module.exports = { add };
