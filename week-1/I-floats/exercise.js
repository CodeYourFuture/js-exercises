var numberOfStudents = 15;
var numberOfMentors = 8;
var total = numberOfStudents + numberOfMentors;

var precentageStudents = Math.round((100 * numberOfStudents) / total);

var precentageMentors = Math.round((100 * numberOfMentors) / total);

console.log(precentageStudents);
console.log(precentageMentors);
