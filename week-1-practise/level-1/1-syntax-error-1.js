// There is a syntax error in this code. Fix it.

function addNumbers(jibran, nini, milli) {
  return nini + jibran + milli;
}
var result = addNumbers(1, 3, 4);
console.log(addNumbers(1, 4, 3)); // Expected result 8 - Run the program and make sure it works

// Answer these questions:
// 1. How many parameters does the function take? // 3
// 2. What is the function name?//addNumbers
// 3. Where is the function called? with what parameters?//variable result &
