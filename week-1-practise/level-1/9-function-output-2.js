function s(w1, w2) {
    return w1.concat(w2);
}

var result = s('code', 'your');
console.log(result);

// Answer these questions:
// - What does this program do? Run it and see, or Google some of the methods used ot understand them.
// - How many parameters does the function take?
  //--> two
// - What is the function name?
  //--> s
// - Where is the function called? with what parameters?
   //-->var result = s('code', 'your'); with (code and your parameter)
// - What does the function return?
   //--> it return word (concatenate the two word)
// - How can this function be improved?
   //--> first give proper function name 
   //--> use + sign instated of using built in function

   /*function Wordconcat(){
       return w1 + w2 ;
   } */