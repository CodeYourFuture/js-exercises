function s(w1, w2) {
  return w1.concat(w2);
}

var result = s("code", "your");
console.log(result);

// Answer these questions:
// - What does this program do? concatenes two strings
// - How many parameters does the function take? two
// - What is the function name? s
// - Where is the function called? with what parameters? is called to assing it to a variable , with "code" and "your" parameters
// - What does the function return? returnes a string
// - How can this function be improved? using an array as a parameter, this way we can do a loop and concatenate more than 2 strings!
