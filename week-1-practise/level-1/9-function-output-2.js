function s(w1, w2) {
  return w1.concat(w2);
}

var result = s("code", "your");
console.log(result);

// Answer these questions:
// - What does this program do? Run it and see, or Google some of the methods used ot understand them.
// It merges two words.
// - How many parameters does the function take? It used two parameters w1 and w2.
// - What is the function name? There are two functions, s and concat.
// - Where is the function called? It is called inside of s function.
//With what parameters? The parameter of concat is w2.
// - What does the function return? The main function merged code and your to new word is (codeyour) .
// - How can this function be improved? It can used it with arrays to merge them and return a new array.
