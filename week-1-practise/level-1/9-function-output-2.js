function s(w1, w2) {
    return w1.concat(w2);
}

var result = s('code', 'your');
console.log(result);

// Answer these questions:
// - What does this program do? Run it and see, or Google some of the methods used ot understand them.
//  the program joins the two strings together.
// - How many parameters does the function take? 2
// - What is the function name? s
// - Where is the function called? with what parameters? in the variable result, with the parameters 'code', 'your'
// - What does the function return? a string 'code your'
// - How can this function be improved? add the two strings together rather than concatenate..... return w1 + w2; 
