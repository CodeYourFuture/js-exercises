function s(w1, w2) {
    return w1.concat(w2);
}

var result = s('code', 'your');
console.log(result);

// Answer these questions:
// - What does this program do? Run it and see, or Google some of the methods used ot understand them.
// - How many parameters does the function take? take 2 parameters
// - What is the function name? s
// - Where is the function called? with what parameters? function was called on line 5 with 'code' and 'word' argument 
// - What does the function return? the function return the two argurment concatenated  
// - How can this function be improved? function name and parameter given meaningful names. the is no need for the concat function, a + sign could do the same job
