function s(w1, w2) {
  return w1.concat(w2);
}

var result = s("code", "your");
console.log(result);

// Answer these questions:
// - What does this program do? Run it and see, or Google some of the methods used ot understand them. Concatenate two strings and returned it as value for variable result.
// - How many parameters does the function take? Two parameters .
// - What is the function name? The function name is "s".
// - Where is the function called? with what parameters?The function called in line 5, with parameters one is "code" the second is "yours".
// - What does the function return? A concatenated strings in this case  the output is "codeyour".
// - How can this function be improved? Give the function more discriptive name.
