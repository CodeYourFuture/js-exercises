function s(w1, w2) {
  return w1.concat(w2);
}

var result = s("code", "your");
console.log(result);

// Answer these questions:
// - What does this program do? Run it and see, or Google some of the methods used to understand them. It conctenates the two words 'code' and 'your' together (puts them together)
// - How many parameters does the function take? as many as possible
// - What is the function name? s
// - Where is the function called? with what parameters? It is assigned to the variable result and it has two strings as parameters "code", "your"
// - What does the function return?  concatenated words 'code your'
// - How can this function be improved? By adding empty strings instead of writing the word. E.g. ''
