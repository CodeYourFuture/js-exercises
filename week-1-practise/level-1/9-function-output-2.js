function s(w1, w2) {
    return w1.concat(w2);
}

var result = s('code', 'your');
console.log(result);

// Answer these questions:
// - What does this program do? Run it and see, or Google some of the methods used ot understand them.  It will concatenate/join the two words.  
// - How many parameters does the function take? This function have 2 parameters.
// - What is the function name? Function name is "s".
// - Where is the function called? with what parameters? line 5 with ('code','your').
// - What does the function return? a string value
// - How can this function be improved? 1- Naming convention(function name isn't meaningful. same for the parameters as well)