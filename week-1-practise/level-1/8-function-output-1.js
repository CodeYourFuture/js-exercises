function getNumber() {
    return Math.random() * 10;
}

var result = getNumber();
console.log(result);

// Answer these questions:
// - What does this program do? Program creates random number between 0 and 1 and then multiply by 10
// - How many parameters does the function take? The function doesn't take any parameters. 
// - What is the function name? getNumber
// - Where is the function called? with what parameters? The function is called on line 5 with no parameters.