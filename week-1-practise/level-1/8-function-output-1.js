function getNumber() {
  return Math.random() * 10;
}

var result = getNumber();
console.log(result);

// Answer these questions:
// - What does this program do? generates a random number and multiply it by 10
// - How many parameters does the function take?0
// - What is the function name? getNumber
// - Where is the function called? with what parameters?var result=getNumber()
