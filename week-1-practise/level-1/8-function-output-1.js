function getNumber() {
    return Math.random() * 10;
}

var result = getNumber();
console.log(result);

// Answer these questions:
// - What does this program do? the math.random will get the  number and times it by 10 and then output the result [The Math.random() function returns a floating-point, pseudo-random number in the range 0–1 ]
// - How many parameters does the function take? 0
// - What is the function name? getNumber
// - Where is the function called? with what parameters? the function is called at line number 5 with no parameters