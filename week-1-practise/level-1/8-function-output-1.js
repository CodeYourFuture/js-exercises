function getNumber() {
  return Math.random() * 10;
}

var result = getNumber();
console.log(result);

// Answer these questions:
// - What does this program do? returns a random number between zero and 1.
// - How many parameters does the function take?no parameter
// - What is the function name?  getNumber
// - Where is the function called? with what parameters? getNumber , no parameter.
