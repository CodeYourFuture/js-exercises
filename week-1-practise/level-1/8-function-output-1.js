function getNumber() {
    return Math.random() * 10;
}

var result = getNumber();
console.log(result);

// Answer these questions:
// - What does this program do? -- it will make a random number and times 10  
// - How many parameters does the function take? --it doesnt get any parameters and it will use a random number
// - What is the function name? getNumber
// - Where is the function called? with what parameters? in line 5 without any parameters