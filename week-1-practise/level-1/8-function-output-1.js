function getNumber() {
  return Math.random() * 10;
}

var result = getNumber();
console.log(result);

// Answer these questions:
// - What does this program do? It multiplies the random number with 10.
// - How many parameters does the function take? It is random.
// - What is the function name? There are two functions getNumber and random.
// - Where is the function called? random function is called insiding getNumber function
//With what parameters? It is just one random parameter everytime the function is run.
