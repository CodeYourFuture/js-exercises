function getNumber() {
  return Math.random() * 10;
}

var result = getNumber();
console.log(result);

// Answer these questions:
// - What does this program do? It returnes a random number
// - How many parameters does the function take? zero
// - What is the function name? getNumber
// - Where is the function called? with what parameters? is called to assing it to a variable,with no parameters
