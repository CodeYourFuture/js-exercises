function getNumber() {
  return Math.random() * 10;
}

var result = getNumber();
console.log(result);

// Answer these questions:
// - What does this program do? choose a random number and times it by 10.
// - How many parameters does the function take? one
// - What is the function name? getNumber
// - Where is the function called? with what parameters? in var result, the parameter is a random number choosen by Maths.random.
