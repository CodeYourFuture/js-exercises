function getNumber() {
    return Math.random() * 10;
}

var result = getNumber();
console.log(result);

// Answer these questions:
// - What does this program do?
//generate random numbers everytime it's called, from 0 to one (not included)
// - How many parameters does the function take?
//none needed as random() will generate the number alone
// - What is the function name?
//getNumber
// - Where is the function called? with what parameters?
//line 6 with console log witout any parameters