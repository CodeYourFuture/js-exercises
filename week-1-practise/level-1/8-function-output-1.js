function getNumber() {
  return Math.random(Math.round()); //
}

var result = getNumber();
console.log(result);

// Answer these questions:
// - What does this program do? // gives random num from 0.,,, upto 9.9999

// - How many parameters does the function take? // nothing
// - What is the function name? //getNumber
// - Where is the function called? with what parameters? // in the variable result n...o
