function getNumber() {
  return Math.random() * 10;
}

var result = getNumber();
console.log(result);

// Answer these questions:
// - What does this program do? picks a random number between 0 and 1 then multiplied by 10.
// - How many parameters does the function take?none
// - What is the function name? getNumber
// - Where is the function called? with what parameters?funtion called at line 5 with no specified parameters
