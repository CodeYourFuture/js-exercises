// There is a syntax error in this code. Fix it.

function addNumbers(a, b, c) {
  return a + b + c;
}
var result = addNumbers(1, 2, 3);
console.log(result);
console.log(addNumbers(1, 3, 4)); // Expected result 8 - Run the program and make sure it works
